import random

def otp(text,key):
    return ''.join(
        chr((ord(c)-65+k)%26+65)
        for c,k in zip(text,key)
    )

text="HELLO"
key=[3,19,5,7,11]

cipher=otp(text,key)

print("Cipher:",cipher)
